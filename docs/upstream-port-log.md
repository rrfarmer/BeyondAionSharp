# Upstream port log

Baseline: Beyond Aion Java `4.8` at `f2f77fefef00aacbef4c10614c18b339bbdaa05a`.

| Upstream SHA | Date | Subject | Status | C# commit / PR | Notes |
|---|---|---|---|---|---|
| `1420a7057` | 2026-05-26 | Refactor stat caps and add dynamic limits (#138) | Ported | `codex/upstream-1420a7057-stat-caps` | Dynamic cap rules and focused player/staff/non-player tests. |
| `cf11ba99a` | 2026-05-26 | Add required dispel count and missing levels to skill_templates.xml (#139) | Ported | `main` | Exact XML/XSD carryover; C# metadata-driven dispel power and target-slot-level fix with focused tests. |
| `c84218bff` | 2026-05-29 | Legendary Symphony event | Ported | `main` | Exact event schedule and SQL carryover; C# reward table, generated syntax, and focused regression tests. |
| `f51eb98a5` | 2026-05-29 | Fixed #BA3735 | Ported | `main` | Cross-slot conflicts limited to matching shield, protect, or reflector effect types; focused conflict tests. |
| `635777e99` | 2026-06-01 | Normalize retail no-resist behavior for selected effect types (#140) | Ported | `main` | Exact 91-type no-resist normalization, cannot-miss integration, MPSHIELD conflicts, and focused tests. |
| `56d461d56` | 2026-06-01 | Fixed selling to certain NPCs | Ported | `main` | Exact NPC XML carryover; all 79 changed records verified through the functional-dialog parser. |
| `7bd02e9d2` | 2026-06-06 | Always resist nofly/fall effects with INVULNERABLE_WING (#141) | Ported | `main` | Calculation-time wing immunity for fall/no-fly effects with safe non-player application and focused tests. |
| `3b3a6b07d` | 2026-06-06 | Fixed GM bookmarks | Ported | `main` | Player-scoped bookmark DAO, client-native command/login flow, exact SQL/config carryover, and packet/access tests. |
| `796a72bd6` | 2026-06-06 | Shield mastery: damage reduce instead of block % (#142) | Ported | `main` | Exact six-skill XML carryover from block to damage reduction, with focused data parity coverage. |
| `6ee1e7b03` | 2026-06-07 | Removed unsupported in-game shop (closes #18) | Ported | `main` | End-to-end shop/toll/premium removal, compacted bridge opcodes and auth packet, plus exact SQL/config carryover; 765 tests pass. |
| `266a073fb` | 2026-06-08 | Load country code specific goodslists (#144) | Ported | `main` | Exact six-region goods-list data, country-aware cache/direct-holder selection with fallback, and client-native trade rejection; 775 tests pass. |
| `9ca6a3753` | 2026-06-13 | Fixed NPC states | Ported | `main` | Exact NPC/spawn state data, obsolete aggro-runner AI removal, and focused template/spawn regression coverage; 781 tests pass. |
| `edeb1a997` | 2026-06-14 | Implemented aerial_spawn attribute | Ported | `main` | Exact spawn/XSD data, runtime aerial flag propagation, retail state precedence, and focused model/controller tests; 786 tests pass. |
| `031a7eec6` | 2026-06-14 | Fixed issues with `//enemy` command | Ported | `main` | Centralized NPC disposition/aggro semantics, corrected command text, and covered all custom-state attackability cases; 794 tests pass. |
| `12851ab5a` | 2026-06-14 | `//moveto` improvements | Ported | `main` | Preserved active instance coordinates, added obstacle-ignoring forward movement, switched teleport correction to absolute position packets, and added focused math/encoding tests; 801 tests pass. |
| `37c93df63` | 2026-06-14 | Fixed XP for NPCs with custom HP via `modifyOwnerStat()` | Ported | `main` | XP now uses runtime MAXHP, with an isolated real-NPC/test-AI regression proving modified HP affects reward; 802 tests pass. |
| `9d32f6172` | 2026-06-16 | feat: instance scaling (#143) | Ported | `main` | Added configurable, grow-only instance NPC HP/damage scaling backed by weak instance state; shared config blob matches Java exactly, focused lifecycle/stat tests added, and all 809 tests pass. |
| `ee8cb3e40` | 2026-06-16 | Decrease the dispel counter only upon successful removal of a buff/debuff (#147) | Ported | `main` | Dispel target count now decreases only after power fully removes an effect in both affected paths; two ordered-candidate regressions added, and all 811 tests pass. |
| `302cd8ac7` | 2026-06-21 | Fix incorrect skill mapping logic for req_dispel attributes (#148) | Ported | `main` | Applied all 376 `req_dispel` metadata reassignments verbatim; XML blob matches Java exactly, representative loader assertions cover removed and corrected IDs, and all 811 tests pass. |
| `59f65a956` | 2026-06-21 | Fixed buff removal when unsocketing stigmas (#BA3708) | Ported | `main` | Skill removal now ends only passive, toggle, deity-avatar, and `WS_` effects, preserving ordinary active stigma buffs; focused eligibility coverage added, and all 812 tests pass. |
| `821014f91` | 2026-08-03 | Summer event | Direct data carryover | main | Exact three-file XML carryover (event schedules, new Increased Enchantment Rate event, decompose chest swap to 188054238/188053698). Verified both enchantment_stone config keys are registered [Property] fields applied via Event.Start Config.Load, single-value 73 override is membership-clamped in Rates.Get, and both new item IDs exist as 4.8 decomposables. Four focused schedule/property/reward tests added; full suite passes (2956 + 4). |
| `003ceba9e` | 2026-08-03 | Updated README.md | Not applicable | main | Java-repo README only: contributors-badge host swap, client download mirror change (mega.nz to archive.org), and start.bat simplification. No BeyondAionSharp doc reproduces that content - grep for mega.nz/version.dll/affinity/aion.bin/loginex across all *.md returns nothing; README.md and RUNNING.md document the C# stack, not game-client setup. |
| `997ac8ac6` | 2026-08-03 | Fixed DB init script | Not applicable | main | Upstream adds the comma missing between PRIMARY KEY and the bookmark_ibfk_1 constraint in aion_gs.sql (a syntax error introduced by 3b3a6b07d). Our GM-bookmarks port already carried the corrected DDL - aion_gs.sql line 115 has the comma and GameServerSchemaSanityTests asserts the exact 'PRIMARY KEY (player_id, name),\n\tCONSTRAINT' sequence - so the tree already matches the Java post-image. |
| `c7918e016` | 2026-08-03 | Added config options to InstaceScaler and set some default restrictions | Ported | main | New scaling knobs (max_level_diff, npc_min_rating, hp/dmg scale factors), dmg_floor default 0.5 to 0.75, rating gate in ShouldScale, scale-factor multiplier formula, and level-diff scaling disable via instance cooltime min entry levels. NpcRating stays implicitly sequential so direct enum comparison equals Java ordinal(), locked by a test; cooltime lookup NREs on missing entry exactly where Java NPEs. Multiplier theory extended with identity and both Java-default factor cases; config-default and rating-order regressions added; full suite passes. |
| `b16943b69` | 2026-08-03 | Set drown period from 2000 ms to 1000 (#158) | Ported | main | DROWN_PERIOD 2000 to 1000 ms and per-tick damage from Math.round(maxHp/10f) to maxHp/20 integer truncation - C# int/int division matches Java exactly (GetMaxHp returns int). Constant locked by reflection regression; the damage lambda itself needs a live player harness, so truncation parity rests on identical int-division semantics. Full suite passes. |
| `872154c27` | 2026-08-03 | Wish commands fix (#152) | Ported | main | Both ///wish item-add and ///wishid now default the recipient to the admin when no player is targeted, instead of rejecting with STR_INVALID_TARGET / target-required messages; canOperate, AddItem, and give/receive notifications all reroute to the resolved target. Line-for-line control-flow mirror of the Java change with reference-equality checks preserved (admin != target in Wishid, !admin.Equals(target) in Wish); console commands have no test harness, so validation is the green full suite plus the structural-fidelity check. |
| `932d0be2a` | 2026-08-03 | Limit signet level explosion and set current signet level for add effect. (#150) | Ported | main | Signet burst now clamps the burst level to min(template signetlvl, stacked signet skill level) - previously a higher stacked level than the template allowed picked oversized SignetData - and records the clamped level via SetSignetBurstedCount so the add effect fires at the actual bursted level (consumed at EffectTemplate line 342). Both C# hooks already existed; no effect-calculation harness exists, so validation is the mirrored three-line change plus the green full suite. |
| `a633e3291` | 2026-08-03 | Fix OnFly condition to require flying state (#154) | Ported | main | OnFlyCondition now checks IsInFlyingState (bit-test against FlyState.FLYING) instead of IsFlying, so gliding no longer satisfies fly-only skill/stat/effect conditions; Player.IsFlying relaxed from flyState >= 1 to flyState != 0 to cover any nonzero fly-state bits. Both members already existed on Creature/Player with Java-parity semantics; validated by the mirrored substitution and the green full suite. |
| `2b843f847` | 2026-08-04 | Fix: update NPC look state after dialog closes (#156) | Ported | main | NpcController now broadcasts SM_LOOKATOBJECT on every target change (previously only in the non-talk branch), and CM_CLOSE_DIALOG gains a null-target guard and sends SM_LOOKATOBJECT(target) after DialogService.OnCloseDialog so NPCs stop staring after a dialog closes. Both files were faithful pre-images untouched by the retail-AI work; no fidelity-log entry applies. Mirrored control flow; validated by the green full suite. |
| `3c7223c01` | 2026-08-04 | Retail-accurate fear/confuse movement behavior (#145) | Ported | main | MoveToPoint now returns bool and redirects the destination when the NPC is already point-moving (refusing only mid-chase), and fear/confuse ticks multiply flee distance by 2.5x movement speed with the ResetMove call dropped, so feared/confused NPCs run continuously as on retail. Kept task classes instead of Java records per local convention. PatternAi.FleeFrom (the retail-AI caller) starts from a stopped state via CancelFlee so its behavior is unchanged; all other call sites ignore the return value like Java's. Full suite including the boss-AI movement harness passes. |
| `2e430a40a` | 2026-08-04 | Fixed ineffective stat caps | Ported | main | Zero-floor cap registration now covers PHYSICAL_ATTACK (previously never capped, making its floor ineffective) and drops the six MAIN_HAND_/OFF_HAND_ stats, which revert to unregistered/unlimited. Exact list mirror; StatCapUtilTests theory rows updated and a regression added asserting the de-registered stats are unlimited both ways. |
| `2d9ed9ced` | 2026-08-04 | Stat calculation refactoring + minor corrections of fallback enum values | Ported | main | Renamed the Stat2-taking GetStat overload to ApplyStatFunctions across CreatureGameStats/NpcGameStats/PlayerGameStats (with the Npc override keeping ModifyOwnerStat), and corrected the no-weapon/wrong-weapon fallback AdditionStat enums: PHYSICAL_ATTACK for p-attack, MAGICAL_ATTACK for m-attack, PHYSICAL_CRITICAL and PHYSICAL_ACCURACY for off-hand crit/accuracy (previously MAIN/OFF_HAND enums, whose caps de-registered last commit); GetOffHandPCritical also moves its base read inside the weapon branch. Line-for-line mirror; golden combat/player/stats formula fixtures all pass, which pins the numeric behavior. |
| `0b10da7fb` | 2026-08-12 | Improved local IPv4 address discovery | Ported | main | FindLocalIPv4 now determines the outbound address by connecting an unbound UDP socket to 1.1.1.1:80 (no packets sent) and reading the OS-chosen local endpoint, replacing first-interface enumeration that could pick an unroutable adapter; failures log and return null exactly like Java. Socket-level infrastructure with idiomatic .NET Socket per the infrastructure exception; behavior contract (outbound IPv4 or null) mirrored. Full suite passes. |
| `f87688531` | 2026-08-22 | Fixed the quest npcs of 29 quests (#166) | Direct data carryover | main | Exact 12-file quest_script_data carryover swapping broken start/end npcs (doors, test dummies, unspawned or wrong-region npcs) for the correct ones across 29 quests. All 25 distinct replacement npc ids verified present in this port's 4.8 npc_templates.xml, so no 5.8-boundary content was introduced; quest script data load is covered by the real-static-data integration tests and the full suite passes. |
| `5deb551af` | 2026-08-22 | Adjust dmg by pvp or pve modifiers for MagicCounterAtk (#151) | Ported | main | MagicCounterAtk damage now runs through StatFunctions.AdjustDamageByPvpOrPveModifiers (with the skill template's pvp_damage and the effect element) before the maxdmg clamp, and the base uses CalculateBaseValue(effect) instead of the raw template value - mirroring Java exactly, including the int cast moving outside the min so the clamp compares floats. All three referenced members (AdjustDamageByPvpOrPveModifiers, CalculateBaseValue, GetPvpDamage) already existed with parity semantics; no effect harness exists, validation is the mirrored change plus the green suite. |
| `5d9c01fcd` | 2026-08-23 | Concentration formula (#170) | Ported | main | Retail interruption formula: chance per mille = damage/maxHp * cancelRate * 100 * (cancelLevel/100) - concentration, truncated, rolled Rnd.Get(1,1000), replacing the factor-7 percent roll, the halved-concentration scaling, and the boss exemption (bosses simply have cancel_level 0). New Creature.GetCancelLevel (players 100) with Npc override reading the new cancel_level template attribute. The 33072 explicit cancel_level values were merged per-npc_id into our npc_templates.xml rather than file-carried, because the retail-AI work owns ~41k diverged lines (ai= and maxHp overrides) that a whole-file carryover would have clobbered; a full id-to-value cross-check confirms our merged attribute set is identical to Java's post-image, and the XSD gained the attribute. Focused regressions cover explicit 0/90 values, the default 100, and the player constant. |
| `b0e897f0c` | 2026-08-25 | Rework item use handling: casting delays, cancel bugs, quest dialogs, retail messages (#153) | Ported | main | Full item-use rework: template-driven casting_delay (new XML attribute, 6103 items) replaces hard-coded delays across all item actions with immediate execution at delay 0; item cooldowns start on successful completion (Player.StartCooldown) instead of optimistically in CM_USE_ITEM; aborts route through the new CancelUseItem(false) overload so cooldowns are no longer wrongly removed; item casts are now interruptible by skill use (PlayerController/PlayerRestrictions) and Skill.calculateCastDuration reads the template delay; QuestStartAction defers eligibility to after the cast with retail messages, CM_USE_ITEM skips the duplicate quest-dialog path, and 26 quest handlers drop their own animation timers; retail STR_USE_ITEM/AP-decompose/charge/polish messages added; ApExtract/Charge/Tampering/Tuning/Polish gained retail validation orders; CompositionAction demoted to a plain CM_COMPOSITE_STONES helper; PortalCooldown counts by amount with getOrCreate; EnchantService/QuestService/Stigma/AbyssPoints/ItemSocket adjusted. Exact item_templates.xml/xsd carryover (byte-verified against Java post-image); three focused template-default/real-data regressions added; full suite and warning baseline pass. |
| `15eda61a2` | 2026-08-25 | Apply item skill effects immediately (#163) | Ported | main | Item skills now apply their effects immediately at cast end instead of after hitTime, with SM_CASTSPELL_RESULT sent beforehand so the client learns the hit time before any HP change arrives (hitTime now only schedules the visual). Mirrored branch restructure in Skill.EndCast; no skill harness exists, validated by the mirrored change plus the green full suite. |
| `4e200e017` | 2026-08-25 | Fixed physical defense penalties applying to the base value only (#164) | Ported | main | Stat2 gains a finalRate multiplier applied to the whole value (base and bonus, set after stat functions so caps ignore it); the flying physical-defense penalty now sets finalRate 0.6 instead of subtracting half the base from the bonus, and both AttackUtil defense reads collapse to GetCurrent() so the movement modifier covers base plus bonus. Golden combat/formula fixtures pass unchanged (finalRate defaults to 1), pinning existing numeric behavior; the flying path has no harness, mirrored line-for-line. |
| `6ffedcd4f` | 2026-08-26 | Enter combat when attacking, not only when being attacked (#175) | Ported | main | Hostile skills (subtype ATTACK or DEBUFF) now refresh the player combat timer: the caster enters combat at cast end and every hit player when effects apply, where IsInCombat previously only saw auto attacks. New PlayerController.EnterCombat(attacking) wraps the two timestamp fields; player-only bookkeeping - the pattern-AI code has zero IsInCombat readers, so no retail-AI interaction. Mirrored line-for-line (caster local renamed to avoid a pre-existing C# scope collision); full suite passes. |
| `3ca785ba4` | 2026-08-26 | Minor packet correction | Ported | main | SM_SUMMON_PANEL now writes physical and magical defense as full DWORDs (previously PDef as a word plus a padded MResist word pair); summon field made readonly. Byte-layout mirrored exactly; full suite passes. |
| `b2e1bb57c` | 2026-08-27 | Reworked `//stat` and `attrbonus` | Ported | main | Full //stat rework: fuzzy stat-name matching (abbreviations like flyt), list/cancel subcommands, per-stat CommandStatOwner overrides via a new CommandStatFunction (priority 120, sets bonus to reach an absolute value), grouped colored function display, and abs applying absolute_stats modifiers per-stat; ///attrbonus now delegates to the Stat command via ChatProcessor.GetCommand<T>. AbsoluteStatOwner deleted with its Player wiring; ChatUtil.LeftPad added. Note: upstream b2e1bb57c does not compile alone - it calls getExactCurrentWithoutBonus which only f6e9a1816 adds - so that 2-line accessor was pulled forward into this port (flagged in Stat2) to keep the build green. Full suite passes. |
